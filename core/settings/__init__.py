# Settings package.
# The active settings module is selected via the DJANGO_SETTINGS_MODULE
# environment variable. Defaults are wired in manage.py / wsgi.py / asgi.py
# to point at `core.settings.development`.
